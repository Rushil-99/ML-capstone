# models/

Saved model artifacts (via `joblib.dump`), optional. `.pkl`/`.joblib` files are git-ignored — save the best model per track here locally, and document which file corresponds to which track/algorithm below.

| File | Track | Algorithm | Notes |
|---|---|---|---|
| | | | |
